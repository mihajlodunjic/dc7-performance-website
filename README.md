# DC7 Performance

Astro/TypeScript static site for Render Static Site deployment.

Build command:

```bash
npm run build
```

Publish directory:

```text
dist
```

Set `SITE_URL` on Render to the production canonical origin.
